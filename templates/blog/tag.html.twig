{% include "blog.html.twig" with {
    pagetitle: 'Tag : ' |trans({}, 'messages') ~ tag.name | capitalize,
    pagination: pagination
} %}

{% block breadcrumb %}
	{% set breadcrumb = [{ "blog": ('Blog'|trans), "current":(pagetitle) }] %}
	{% include "global/breadcrumb.html.twig" with breadcrumb %}
{% endblock %}
